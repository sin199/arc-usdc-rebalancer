// The submitted deployment is a read-only DeFi decision MVP.
// Keep this invariant explicit so a wallet cannot turn the public demo into a write surface.
export const publicReadOnlyDeployment = true
